import './NotFound.css';

function NotFound() {
    return (
        <div className={'not-found'}>
            <img src={'/images/404NotFound.png'} alt={'not found'} />
        </div>
    );
}

export default NotFound;